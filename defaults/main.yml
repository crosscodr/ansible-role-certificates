---
certificates_dir: "{{ role_path }}/vars"


# directory where certs are deployed to
certificates_path_owner: root
certificates_path_group: root

# certificates_cert_owner: root
# certificates_cert_group: root
certificates_privkey_owner: root
certificates_privkey_group: "{{'ssl-cert' if ansible_os_family == 'Debian' else 'root' }}"
